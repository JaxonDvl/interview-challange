export {}
describe("convertBalancesToCurrency", () => {
    // TODO add at least 3 relevant units tests
    it("should convert balances to USD", () => {
        const result = 0
        expect(result).toBe(0);
    });
});